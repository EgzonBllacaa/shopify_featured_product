<section>
    <div class="product-section">
        <div class="product-images">
            {% if product.images.size > 0 %}
                {% for image in product.images %}
                    {% if image != blank %}
                        <img src="{{ image | image_url: width: 100, height: 200 }}" class="product-image" width="100" height="200" alt="">
                    {% endif %}
                {% endfor %}    
            {% else %}
                {{ "product-1" | placeholder_svg_tag }}
            {% endif %}
        </div>
        <div class="product-content">
            <div class="product-content_title">
                {% if product.title != blank %}
                    <h2>{{ product.title }}</h2>
                {% endif %}
                {% if product.vendor != blank %}
                    <span>{{ product.vendor }}</span>
                {% endif %}
                    <div>
                        {% if product.compare_at_price != blank and product.compare_at_price > product.price %}
                            <div style="display: flex; gap: 10px">
                                <span>Discount: {{ product.price | money }}</span>
                                <span class="product-old-price">Original Price: {{ product.compare_at_price | money }}</span>
                            </div>
                            {% else %}
                            <span>Price: {{ product.compare_at_price | money }}</span>
                        {% endif %}
                            <div>
                            {% comment %} {{- product. | inline_asset_content -}} {% endcomment %}
                        </div>
                    </div>
                    <div style="display: flex; flex-direction: column">
                        <span style="text-decoration: underline">What's My Size?</span>
                        <span style="text-decoration: underline">Size Guide</span>
                    </div>
            </div>
            <div class="product-content_variants">
                <div>
                    <span>Fit</span>
                    {% if product.variants.size > 0 %}
                        <div class="product-fit_colors">

                            {% for variant in product.variants %}
                                <div class="product-variants">
                                    {{ variant.option1  }}
                                </div>
                            {% endfor %}
                        </div>
                        {% endif %}
                </div>
                <div>
                    <span>Colors:</span>
                    {% if product.variants.size > 0 %}
                        <div class="product-fit_colors">
                            {% for variant in product.variants %}
                                
                                <div class="product-variants" style="background-color: {{ variant.option2 | downcase }}; padding: 10px 10px">
                                    
                                </div>
                            {% endfor %}
                        </div>
                        {% endif %}
                </div>
                <div>
                    <span style="display: block;">Tags</span>
                    {% if product.tags != nil %}
                        {% for tag in product.tags %}
                            <button class="product-variants button_class">{{ tag }}</button>
                        {% endfor %}
                    {% endif %}
                    </div>
                <div>
                    {% if product.available == true %}
                        <div class="product-checkout">
                            <div>
                                <button>Add to bag</button>
                                {% comment %} Heart Icon here {% endcomment %}
                            </div>
                            <div class="product-free_delivery">
                                <div>
                                    <button>Free delivery</button>
                                    <button>Free Returns</button>
                                </div>
                                {% comment %} dropdown icon that i can't figure out how to import :) {% endcomment %}
                            </div>
                        </div>
                    {% else %}
                        <button>Sold out</button>
                    {% endif %}
                </div>
                <div>
                    <p>Product Details</p>
                    <p>
                        {% if product.description != blank %}
                            {{ product.description }}
                        {% endif %}
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>
{% stylesheet %}
    .product-section {
        width: 100%;
        display: flex;
        gap: 40px;
        justify-content: center;
    }
    .product-variants {
        border: 1px solid black; 
        padding: 10px 20px;
        border-color: gray;
    }
    .product-variants.button_class {
        background: white;
    }
    .product-old-price {
        text-decoration: line-through;
        color: red;
    }
    .product-images {
        width: 50%;
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
    }
    .product-checkout div:first-child button {
        width: 100%;
        background-color: black;
        color: white;
        padding: 10px 0;
        font-weight: medium;
        border: none;
    }
    .product-checkout div:last-child {
        display: flex;
        border-left: 0px;
        width: 100%;
    }
    .product-checkout div:last-child button {
        width: 100%;
        padding: 10px 15px;
        border: 1px solid gray;
        color: black;
        background: none;
    }
    .product-content {
        width: 50%;
    }
    .product-content_title {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }
    .product-checkout {
        margin: 30px 0;
    }
    .product-content:first-child {
        margin: 0;
    }
    .product-content.product-content_variants {
        margin: 30px 0;
    }
    .product-image {
        width: 300px;
    }
    .product-fit_colors {
        display: flex;
        gap: 10px; 
        margin: 2px 0px;
        flex-wrap: wrap;
    }
{% endstylesheet %}

<script type="text/javascript">
  console.log({{ product | json }});
</script>


{% schema %}
    {
        "name": "Featured Product",
        "settings": [
            {
                "type": "text",
                "id": "product_title",
                "label": "title",
                "default": "First Product"
            },
            {
                "type": "number",
                "id": "product_price",
                "label": "Product Price",
            },
            {
                "type": "image_picker",
                "id": "image",
                "label": "Select new images",
            },
            {
                "type": "richtext",
                "id": "content",
                "label": "Adjust the description",
            },

        ],
        "presets": [
            {
                "name": "Featured Product"
            }
        ]
    }
{% endschema %}